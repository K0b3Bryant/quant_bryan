CONFIG = {
    'date_format': '%Y-%m-%d',  # Format of the date column
    'lag': 1,  # Lag for positions (default is 1 day)
    'transaction_cost': 0.001,  # Transaction costs as a percentage
    'rolling_window': 20  # Rolling window size for metrics
}
